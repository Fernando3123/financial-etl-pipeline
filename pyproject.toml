[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "financial_monitoring_engine"
version = "0.1.0"
description = "Motor de risco e monitoramento financeiro"
authors = [{name = "Fernando", email = "seu_email@exemplo.com"}]
dependencies = [
    "pandas",
    "yfinance",
    "numpy"
]

# Aqui está a mágica: dizemos que o código fonte está na pasta src
[tool.setuptools.packages.find]
where = ["src"]